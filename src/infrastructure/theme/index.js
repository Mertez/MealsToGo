import {colors, standardcolors} from "./colors";
import {spaces, lineHeight} from "./spacing";
import {sizes} from "./sizes";
import {fonts, fontWeights, fontSizes} from "./fonts"

export const theme = {
    colors,
    standardcolors,
    spaces,
    lineHeight,
    sizes,
    fonts,
    fontWeights,
    fontSizes
};